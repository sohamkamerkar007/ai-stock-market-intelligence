export function connectMarket(onData,onState){let ws,attempt=0,timer;const connect=()=>{const protocol=location.protocol==='https:'?'wss':'ws';ws=new WebSocket(`${protocol}://${location.host}/ws/market`);ws.onopen=()=>{attempt=0;onState?.('connected')};ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.type==='market_snapshot')onData(m.payload)};ws.onclose=()=>{onState?.('reconnecting');timer=setTimeout(connect,Math.min(30000,1000*2**attempt++))};ws.onerror=()=>ws.close()};connect();return()=>{clearTimeout(timer);ws?.close()}}
